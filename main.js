const metricConverter = require('./p1.js');

console.log(metricConverter.kilometersToMeters(10)); // Output: 10000
console.log(metricConverter.metersToKilometers(10000)); // Output: 10
console.log(metricConverter.centimetersToMeters(100)); // Output: 1
console.log(metricConverter.metersToCentimeters(1)); // Output: 100